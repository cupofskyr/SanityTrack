
"use client";

import AISetupAssistant from '@/components/ai-setup-assistant';
import { useState } from 'react';
import { useToast } from "@/hooks/use-toast";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";
import { Badge } from '@/components/ui/badge';
import { ListTodo } from 'lucide-react';

type ManagedTask = {
    id: number;
    description: string;
    frequency: string;
};

export default function EquipmentPage() {
    const { toast } = useToast();
    const [managedTasks, setManagedTasks] = useState<ManagedTask[]>([]);
    
    const handleGeneratedTasks = (generatedTasks: { description: string, frequency: string }[]) => {
        const newTasks = generatedTasks.map((task, index) => ({
            id: (managedTasks.length > 0 ? Math.max(...managedTasks.map(t => t.id)) : 0) + index + 1,
            description: task.description,
            frequency: task.frequency,
        }));

        setManagedTasks(prevTasks => [...newTasks, ...prevTasks]);

        toast({
            title: "Master Task List Generated!",
            description: `${newTasks.length} new recurring tasks have been added. These will be used in shift planning.`,
        });
    };

    return (
        <div className="space-y-6">
            <AISetupAssistant onTasksGenerated={handleGeneratedTasks} />

            <Card>
                <CardHeader>
                    <CardTitle className="font-headline flex items-center gap-2"><ListTodo /> Master Task List</CardTitle>
                    <CardDescription>
                        This is the master list of recurring tasks generated by the AI. This list will be used by the Shift Planner to automatically assign duties.
                    </CardDescription>
                </CardHeader>
                <CardContent>
                    <Table>
                        <TableHeader>
                            <TableRow>
                                <TableHead className="w-[80%]">Task Description</TableHead>
                                <TableHead>Frequency</TableHead>
                            </TableRow>
                        </TableHeader>
                        <TableBody>
                            {managedTasks.length > 0 ? (
                                managedTasks.map((task) => (
                                <TableRow key={task.id}>
                                    <TableCell className="font-medium">{task.description}</TableCell>
                                    <TableCell>
                                        <Badge variant="secondary">{task.frequency}</Badge>
                                    </TableCell>
                                </TableRow>
                                ))
                            ) : (
                                <TableRow>
                                <TableCell colSpan={2} className="text-center h-24">
                                    No tasks generated yet. Use the AI Setup Assistant above to create a master task list.
                                </TableCell>
                                </TableRow>
                            )}
                        </TableBody>
                    </Table>
                </CardContent>
            </Card>
        </div>
    );
}
