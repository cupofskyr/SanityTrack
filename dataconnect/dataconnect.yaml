specVersion: "v1"
serviceId: "y"
location: "us-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "leifurai-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
